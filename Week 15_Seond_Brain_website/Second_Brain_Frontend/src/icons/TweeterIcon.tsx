interface TweeterIconProps {
    size: "sm" | "md" | "lg";
}

const sizeVariants = {
    sm: "size-3",
    md: "size-4",
    lg: "size-6",
};

export const TweeterIcon = ({size} : TweeterIconProps) => {
    
}